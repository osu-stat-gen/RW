% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RWR.R
\name{RWR}
\alias{RWR}
\title{Random Walk with Restart (RWR) function}
\usage{
RWR(normmatrix, alpha = 0.1, max_iter = 300, tol = 1e-06)
}
\arguments{
\item{normmatrix}{Numeric symmetric square matrix \eqn{P} with each row/column summing to 1
(doubly-stochastic). This function does not renormalize; ensure the matrix
is normalized beforehand (e.g., by \code{KRnorm2()} function).}

\item{alpha}{Restart probability in \eqn{(0,1)}. Default: \code{0.1}.}

\item{max_iter}{Maximum number of iterations. Default: \code{300}.}

\item{tol}{Convergence tolerance on the Frobenius norm of the iterate
difference. Default: \code{1e-6}.}
}
\value{
A numeric matrix \code{Q} of the same dimension as \code{normmatrix}.
}
\description{
Computes the steady-state random-walk-with-restart matrix \eqn{Q} on a symmetric
doubly-stochastic transition matrix \eqn{P} (here \code{normmatrix}), by fixed-point
iteration:
\deqn{Q_{t+1} = (1-\alpha)\, Q_t P + \alpha I, \qquad Q_0 = I.}
At convergence, \eqn{Q} solves \eqn{Q = \alpha(I - (1-\alpha)P)^{-1}} (when
the inverse exists). Each column of \eqn{Q} corresponds to the diffusion
starting at one node with restart probability \eqn{\alpha}.
}
\details{
For better numerical behavior, ensure \code{normmatrix} is nonnegative, symmetric, and
stochastic (\code{rowSums(normmatrix) == 1} within small tolerance). The
iteration stops when \eqn{||Q_{t+1} - Q_{t}||_{F} < }\code{tol} or when \code{max_iter} is reached.
}
\examples{
data(sim1.P.KR)
# Random walk with restart where restart probability is 0.1
Q <- RWR(sim1.P.KR, alpha=0.1, max_iter=300, tol=1e-6)

}
