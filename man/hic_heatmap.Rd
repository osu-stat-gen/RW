% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hic_heatmap.R
\name{hic_heatmap}
\alias{hic_heatmap}
\title{Heatmap visualization of Hi-C data with optional domain boundaries overlay}
\usage{
hic_heatmap(
  hic,
  trunc = FALSE,
  thres = 20,
  title = "",
  add.bound = FALSE,
  bound = NULL,
  col = "blue"
)
}
\arguments{
\item{hic}{Numeric, square matrix of Hi-C contact counts (non-negative).}

\item{trunc}{Logical; if \code{TRUE}, values larger than \code{thres} are clipped
before plotting. Default \code{FALSE}.}

\item{thres}{Positive number used when \code{trunc = TRUE} to cap large values.
Default \code{20}.}

\item{title}{Character plot title. Default empty string.}

\item{add.bound}{Logical; if \code{TRUE}, draw boundary line segments that mark
blocks defined by \code{bound}. Default \code{FALSE}.}

\item{bound}{Optional increasing integer vector of domain boundary positions
(end indices of consecutive blocks). For example, if \code{nrow(hic) = 100}
and \code{bound = c(20, 55, 100)}, the diagonal blocks are \verb{(1,...,20)}, \verb{(21,...,55)},
\verb{(56,...,100)}. If \code{add.bound = TRUE}, \code{bound} must be supplied.}

\item{col}{Color for boundary lines. Default \code{"blue"}.}
}
\value{
A \code{ggplot} object (heatmap), optionally with boundaries overlaid.
}
\description{
Plot a symmetric Hi-C contact matrix as a heatmap (upper-left origin),
with optional truncation of large values and optional overlay of
domain boundaries.
}
\details{
For display, the matrix is transposed and the y-axis is flipped so that
bin \verb{(1,1)} appears in the top-left (conventional Hi-C view).

If \code{bound} vector does not include the last bin (\code{nrow(hic)}), it will
be appended internally for drawing the complete outline.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  data(sim2.E)
  data(sim2.true.bound.E)

  # Heatmap without TAD boundaries
  p1 <- hic_heatmap(sim2.E, trunc=TRUE, thres=30, add.bound=FALSE, title="Sim2 heatmap")

  # Heatmap with true TAD boundaries
  p2 <- hic_heatmap(sim2.E, trunc=TRUE, thres=30, title="Sim2 heatmap with TADs",
                    add.bound=TRUE, bound = sim2.true.bound.E, col="blue")
}

}
