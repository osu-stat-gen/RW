% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RWS.R
\name{RWS}
\alias{RWS}
\title{Random Walk with limited number of steps (RWS) function}
\usage{
RWS(normmatrix, step = 3)
}
\arguments{
\item{normmatrix}{Numeric symmetric square matrix \eqn{P} with each row/column summing to 1
(doubly-stochastic). The function does not renormalize; ensure the matrix
is normalization beforehand (e.g., by \code{KRnorm2()} function).}

\item{step}{Positive integer number of steps \eqn{k} (default: \code{3}).}
}
\value{
A numeric matrix \code{Q} of the same dimension as \code{normmatrix}, equal to \eqn{P^k}.
}
\description{
Computes the \eqn{k}-step transition matrix \eqn{Q = P^k} on a symmetric
doubly-stochastic transition matrix \eqn{P} (here \code{normmatrix}) by repeated
multiplication. This corresponds to a random walk of \code{step}
transitions on the underlying graph/Markov chain.
}
\details{
This is a straightforward dense computation of \eqn{P^k} via repeated
\code{\%*\%}. For large matrices and/or large \code{step}, consider sparse matrices
(e.g., via the \strong{Matrix} package) or fast exponentiation.
}
\examples{
data(sim1.P.KR)
# Random walk at 3 steps
Q <- RWS(sim1.P.KR, step=3)
Q

}
